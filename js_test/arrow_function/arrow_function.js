const foo = (x, y) => {
  console.log('2줄 이상')
  return x + y;
}

// const foo = (x,y) => x + y;
// return 하나일때 중괄호 생략가능
// console.log(foo(1,6));



console.log(foo(1, 5));

//터미널에 node arrow_function.js

//터미널에 cd .. (이전폴더가기)